html
  head
    title Defaulter
    link(rel="stylesheet" href="../../css/pages/onboarding.css")
    link(href='https://fonts.googleapis.com/css?family=Roboto:400,500,700,900&display=swap' rel='stylesheet')
    meta(charset="UTF-8")
  body
    .header.horizontal
      a.back#js-back 🠐 Choose Sites
      div.logo-small
      div.forward
    .horizontal.centered
      #off_by_default.contents
        h1 Defaulter
        button.toggle.spacing
          div.left.on
          div.right
        p The Defaulter turns your Nudge sites off by default, so that instead of visiting out of habit, you get to decide whether you want to visit at all.
        p To visit the site, you have to drag the slider across.
        h3 Options:
        .horizontal.spacing
          .form-checkbox
            input#get_stickier(type="checkbox")
            label(for="get_stickier") Make slider harder to drag across each time
        .horizontal.onboarding-bar.spacing.full-width
          button.primary#js-onboarding-enable Enable the Defaulter
          button.secondary#js-onboarding-skip Skip this feature
        .horizontal.onboarding-bar.spacing.full-width(style="display:none")
          button.primary.pink#js-next Next: Hider
        //- button.popup-button Turn the Defaulter on
    .horizontal.centered.onboarding-preview
      .left
      .center
        img(src="../../img/preview/defaulter2.gif").preview-image
      .right


  script(src="../../config.js")
  script(src="../../js/background/storage.js")
  script(src="../../js/helpers.js")
  script(src="../../js/vars/vars.js")
  script(src="../../js/option.js")